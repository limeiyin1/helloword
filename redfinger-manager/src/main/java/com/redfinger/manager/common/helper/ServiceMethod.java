package com.redfinger.manager.common.helper;

public enum ServiceMethod {
	pre_save, after_save, pre_update, after_update, pre_start, after_start, pre_stop, after_stop, pre_delete, after_delete,
	pre_remove,after_remove,
	pre_remove_single,after_remove_single,
	after_device_vm
	;
}
