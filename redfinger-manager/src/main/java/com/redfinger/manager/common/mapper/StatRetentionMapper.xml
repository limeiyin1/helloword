<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.redfinger.manager.common.mapper.StatRetentionMapper">
	<resultMap id="BaseResultMap" type="com.redfinger.manager.modules.stat.base.StatDomain">
		<result column="id" jdbcType="INTEGER" property="id" />
		<result column="number" jdbcType="INTEGER" property="number" />
		<result column="amount" jdbcType="DOUBLE" property="amount" />
		<result column="label" jdbcType="VARCHAR" property="label" />
	</resultMap>
	
	<select id="statRetention" parameterType="com.redfinger.manager.modules.stat.base.StatDomain" resultMap="BaseResultMap">
		<![CDATA[
		select count(1) as number from 
			(SELECT count(1) as num ,user_id from 
				(select user_id,date_trunc('day', login_time) as time  from sys_login_log  
					where login_time>=to_date('${begin}','yyyy-MM-DD')  and login_time <(to_date('${end}','yyyy-MM-DD')+ integer '1')  
						GROUP BY user_id,time) l GROUP BY user_id ) m 
							where m.num>=2 or 
									user_id in (select user_id from sys_login_log where login_time < to_date('${begin}','yyyy-MM-DD'))
		
	
		]]> 			
		
	</select>
	
	<select id="countAll" parameterType="com.redfinger.manager.modules.stat.base.StatDomain" resultMap="BaseResultMap">
		<![CDATA[
		select count(1) as number from (select user_id from (select user_id,date_trunc('day', login_time) as time  from sys_login_log  where login_time <to_date('${end}','yyyy-MM-DD') and login_time >to_date('${begin}','yyyy-MM-DD')  GROUP BY user_id,time )m group by user_id)t
		
		]]> 			
		
	</select>
	
	
	
</mapper>