<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.redfinger.manager.common.mapper.StatActiveUserMapper">
	<resultMap id="BaseResultMap" type="com.redfinger.manager.modules.stat.base.StatDomain">
		<result column="id" jdbcType="INTEGER" property="id" />
		<result column="number" jdbcType="INTEGER" property="number" />
		<result column="amount" jdbcType="DOUBLE" property="amount" />
		<result column="label" jdbcType="VARCHAR" property="label" />
	</resultMap>
	
	<select id="statActiveUser" parameterType="com.redfinger.manager.modules.stat.base.StatDomain" resultMap="BaseResultMap">
		<![CDATA[
		select 1 as number,user_id from  
			(select user_id,date_trunc('day', login_time) as time  from sys_login_log  where login_time >= to_date('${begin}','yyyy-MM-DD') and login_time < (to_date('${end}','yyyy-MM-DD')+ integer '1')   GROUP BY user_id,time) l GROUP BY user_id HAVING count(1)>=10 ORDER BY user_id
				
				
		]]>
	</select>
	
	<!-- <select id="countAll" parameterType="com.redfinger.manager.modules.stat.base.StatDomain" resultMap="BaseResultMap">
		<![CDATA[
		
		select count(1) as number from (select user_id from log_user where create_time < to_date('${end}','yyyy-MM-DD') group by user_id) as m
		
		]]> 			
		
	</select> -->
	
</mapper>